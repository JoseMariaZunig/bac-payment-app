// Este archivo se regenera automáticamente al arrancar el contenedor
// (ver docker-entrypoint.sh), tomando el valor de la variable de entorno
// API_BASE_URL definida en docker-compose.yml. El valor de aquí es solo
// un default para desarrollo local sin Docker.
window.APP_CONFIG = {
  API_BASE_URL: "http://localhost:8080"
};
